import React from 'react';

// There will be a lot of ranks base on the level and also the animation will be different, inside rank we will have sub ranks

interface RankAnimationProps {
  rank: number;
}

export default function RankAnimation() {
  return;
}
